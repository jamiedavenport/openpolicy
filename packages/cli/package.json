{
	"name": "@openpolicy/cli",
	"version": "0.0.26",
	"type": "module",
	"description": "CLI to install OpenPolicy and print a setup prompt for coding agents",
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/jamiedavenport/openpolicy",
		"directory": "packages/cli"
	},
	"files": [
		"dist",
		"README.md",
		"NOTICE.md"
	],
	"bin": {
		"openpolicy": "./dist/cli.js"
	},
	"scripts": {
		"dev": "rolldown -c --watch",
		"build": "rolldown -c && chmod +x dist/cli.js",
		"check-types": "tsc --noEmit",
		"test": "bun test"
	},
	"devDependencies": {
		"@openpolicy/tooling": "workspace:*",
		"citty": "^0.1.6",
		"consola": "^3.4.2"
	}
}
